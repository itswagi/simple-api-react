export enum ActionType {
  IDLE = 'idle',
  LOADING = 'loading',
  FULFILLED = 'fulfilled',
  REJECTED = 'failed',
}

export enum StatusEnum {
  SEARCH = 'search',
  EVENTS = 'events',
  SHOWEVENTS = 'showEvents',
}
